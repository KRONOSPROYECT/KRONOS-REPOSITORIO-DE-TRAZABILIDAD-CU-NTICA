import './globals.css'
export const metadata = { title: 'KRONOS 360', description: 'Trazabilidad Cuántica Offline' }
export default function RootLayout({children}:{children:React.ReactNode}){ return <html lang="es"><body className="bg-[#050505] text-white">{children}</body></html> }
