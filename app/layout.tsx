import "./globals.css"
export const metadata = { title: "KRONOS 360 | Trazabilidad Cuántica Offline", description: "Kernel Offline 72 - Verificación ORIGINAL/CLON" }
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="es"><body className="bg-[#050505] text-white antialiased">{children}</body></html>
}
